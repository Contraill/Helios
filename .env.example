# Helios — server environment variables.
# Copy to .env.local for local development. Never commit real values.
#
# NASA Open APIs key (https://api.nasa.gov).
# Server-only by design (see src/lib/env) — NEXT_PUBLIC_NASA_API_KEY is rejected.
# Optional until Phase 7 (first NASA integrations); required from Phase 7 on.
# NASA_API_KEY=
